define('webpack/node_modules/watchpack/node_modules/chokidar/node_modules/glob-parent/index', function(require, exports, module) {

  'use strict';
  
  var path = require('path');
  var isglob = require('webpack/node_modules/watchpack/node_modules/chokidar/node_modules/is-glob/index');
  
  module.exports = function globParent(str) {
  	str += 'a'; // preserves full path in case of trailing path separator
  	do {
  		str = path.dirname(str);
  	} while (isglob(str));
  	return str;
  };

});
